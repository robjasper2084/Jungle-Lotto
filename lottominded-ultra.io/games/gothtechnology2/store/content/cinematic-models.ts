/** Populate only after the owner's to3D exports have been inspected locally. */
export const cinematicModels: { hoodie: string | null; charm: string | null; mobsterCharm: string | null } = {
  hoodie: null,
  charm: null,
  mobsterCharm: 'models/mobster-luggage-charm.glb',
};
