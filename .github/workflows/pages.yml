name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'lottominded-ultra.io/games/gothtechnology2/**'
      - 'lotto mind refined/**'
      - 'lottominded-ultra.io/games/opengw-levels/**'
      - 'scripts/*pages*.mjs'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: lottominded-ultra.io/games/gothtechnology2/package-lock.json
      - name: Install GothTechnology dependencies
        working-directory: lottominded-ultra.io/games/gothtechnology2
        run: npm ci
      - name: Validate and build GothTechnology storefront
        working-directory: lottominded-ultra.io/games/gothtechnology2
        env:
          PUBLIC_COMMERCE_MODE: demo
          PUBLIC_LAUNCH_APPROVED: 'false'
        run: |
          npm run check:store
          npm run test:store
          npm run check
          npm run test:unit
          npm run build
      - name: Install browser for storefront regressions
        working-directory: lottominded-ultra.io/games/gothtechnology2
        run: npx playwright install --with-deps chromium
      - name: Verify game portal, artwork and video controls
        working-directory: lottominded-ultra.io/games/gothtechnology2
        run: npm run test:store:browser -- --project=desktop --grep "game portal|homepage category tile|homepage signal row|collection signal columns|commercial|Lookbook films preview|inline film native controls|reduced motion and save-data|header music control|hero background|fragrance stays|fragrance concept stories|charm and knife preview|Original Artwork"
      - name: Verify New Drop Underground popup on desktop and mobile
        working-directory: lottominded-ultra.io/games/gothtechnology2
        run: npm run test:store:browser -- --project=desktop --project=mobile --grep "New Drop Underground|game popup placement"
      - name: Verify shared game rewards and discount prices
        working-directory: lottominded-ultra.io/games/gothtechnology2
        run: npm run test:store:browser -- --project=desktop --project=mobile --grep "shared game reward|Underground discount|Game Grid|C-Files removed"
      - name: Validate Pages assembly
        run: node --test scripts/gothtechnology-pages.test.mjs
      - name: Build lean Pages artifact
        run: node scripts/build-pages-artifact.mjs
      - name: Verify packaged arcade assets on desktop and mobile
        run: node scripts/gothtechnology-pages-browser.mjs
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
